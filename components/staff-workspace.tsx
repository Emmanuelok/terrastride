'use client';
import {useCallback,useEffect,useState,useRef} from 'react';
import {ArrowLeft,ArrowRight,RefreshCw,Inbox} from 'lucide-react';
type RequestRow={id:string;kind:string;status:string;created:string;data:Record<string,any>;messages?:{id:string;message?:string;body?:string;created:string}[]};
export default function StaffWorkspace({onAccount}:{onAccount:()=>void}){
 const detailSequence=useRef(0),operation=useRef<{payload:string;key:string}|null>(null);
 const [rows,setRows]=useState<RequestRow[]>([]),[next,setNext]=useState<string|null>(null),[statuses,setStatuses]=useState<string[]>([]),[filter,setFilter]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false),[selected,setSelected]=useState<RequestRow|null>(null),[reply,setReply]=useState(''),[notice,setNotice]=useState(''),[detailReady,setDetailReady]=useState(false);
 const load=useCallback(async(cursor='')=>{setBusy(true);setError('');try{const q=new URLSearchParams({limit:'20'});if(cursor)q.set('cursor',cursor);if(filter)q.set('status',filter);const r=await fetch('/api/admin?'+q);const d:any=await r.json();if(!r.ok)throw Error(d.error||'Staff access is required.');setRows(old=>cursor?[...old,...d.requests]:d.requests);setNext(d.nextCursor);setStatuses(d.statuses||[])}catch(e){setError(e instanceof Error?e.message:'Unable to load requests.')}finally{setBusy(false)}},[filter]);
 useEffect(()=>{load()},[load]);
 const open=async(row:RequestRow)=>{const sequence=++detailSequence.current;setSelected(row);setDetailReady(false);setReply('');setNotice('');setError('');try{const r=await fetch('/api/admin?id='+encodeURIComponent(row.id));const d:any=await r.json();if(sequence!==detailSequence.current)return;if(!r.ok)throw Error(d.error||'Unable to load request.');setSelected({...d.request,messages:d.messages});setDetailReady(true)}catch(e){if(sequence===detailSequence.current)setError(e instanceof Error?e.message:'Unable to load request. Please select it again.')}};
 const update=async(body:Record<string,string>)=>{if(!selected||!detailReady)return;setBusy(true);setError('');setNotice('');try{const payload=JSON.stringify({...body,id:selected.id});if(operation.current?.payload!==payload)operation.current={payload,key:crypto.randomUUID()};const r=await fetch('/api/admin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({...body,id:selected.id,idempotencyKey:operation.current.key})});const d:any=await r.json();if(!r.ok)throw Error(d.error||'Unable to save.');operation.current=null;await open(selected);await load();setReply('');setNotice(body.action==='customer-reply'?'Reply saved to the customer’s request.':'Request status updated.')}catch(e){setError(e instanceof Error?e.message:'Unable to save.')}finally{setBusy(false)}};
 return <section className="section staff-workspace"><button className="text-button" onClick={onAccount}><ArrowLeft size={16}/>My account</button><div className="page-intro"><span className="eyebrow orange">STRIDE OPERATIONS</span><h1>Every request.<br/>A next step.</h1><p>Review enquiries, update their status and leave customer-visible replies.</p></div><div className="staff-toolbar"><label>Request status<select value={filter} onChange={e=>setFilter(e.target.value)}><option value="">All statuses</option>{statuses.map(s=><option key={s}>{s}</option>)}</select></label><button className="button outline-button" disabled={busy} onClick={()=>load()}><RefreshCw size={16}/>Refresh</button></div>{error&&<p role="alert" className="auth-error">{error}</p>}{notice&&<p role="status">{notice}</p>}<div className="staff-layout"><div className="staff-requests">{rows.map(row=><button key={row.id} disabled={busy} className={'staff-request '+(selected?.id===row.id?'active':'')} onClick={()=>open(row)}><span className="eyebrow">{row.kind}</span><strong>{row.data.name||'Customer request'}</strong><span>{row.data.service||row.id}</span><small>{row.status} · {new Date(row.created).toLocaleDateString()}</small><ArrowRight size={17}/></button>)}{!rows.length&&!busy&&!error&&<div className="empty-state"><Inbox size={35}/><h2>Your inbox is clear.</h2><p>New enquiries will appear here.</p></div>}{next&&<button className="button outline-button" disabled={busy} onClick={()=>load(next)}>Load more</button>}</div><div className="staff-detail">{selected?<><span className="eyebrow">{selected.id}</span><h2>{selected.data.service||'Product quotation'}</h2><p><strong>{selected.data.name}</strong><br/>{selected.data.email}<br/>{selected.data.phone}<br/>{[selected.data.city,selected.data.address,selected.data.digitalAddress].filter(Boolean).join(' · ')}</p><p>{selected.data.message}</p>{selected.data.items&&<ul>{selected.data.items.map((item:any)=><li key={item.id}>{item.quantity} × {item.name}</li>)}</ul>}<label>Update status<select value={selected.status} disabled={busy||!detailReady} onChange={e=>update({action:'status',status:e.target.value})}>{[...new Set([selected.status,...statuses])].map(s=><option key={s}>{s}</option>)}</select></label><div className="request-messages">{selected.messages?.map(m=><article key={m.id}><small>{new Date(m.created).toLocaleString()}</small><p>{m.message||m.body}</p></article>)}</div><form onSubmit={e=>{e.preventDefault();update({action:'customer-reply',message:reply.trim()})}}><label>Reply visible to the customer<textarea disabled={busy||!detailReady} value={reply} onChange={e=>setReply(e.target.value)} required minLength={1} maxLength={3000} rows={5}/></label><p className="muted">Replies are saved to My requests. Email delivery depends on the configured notification service.</p><button className="button orange-button" disabled={busy||!detailReady||!reply.trim()}>Save reply<ArrowRight size={17}/></button></form></>:<div className="empty-state"><Inbox size={35}/><h2>A clear view of the details.</h2><p>Select a request to review it.</p></div>}</div></div></section>;
}
