import {products} from '@/lib/catalogue';
export async function GET(){const cols=['id','name','brand','department','category','priceGHS','sourcePrice','sourceCurrency','priceType','sourceName','sourceUrl','image'];const cell=(v:unknown)=>'"'+String(v??'').replace(/"/g,'""').replace(/^[=+@-]/,"'")+'"';const csv=[cols.join(','),...products.map(p=>cols.map(k=>cell(p[k as keyof typeof p])).join(','))].join('\r\n');return new Response('\uFEFF'+csv,{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':'attachment; filename="STRIDE-Complete-Product-Catalogue.csv"'}})}
